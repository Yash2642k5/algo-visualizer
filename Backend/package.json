{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "setup": "npx npm-run-all build-frontend install-backend",
    "build-frontend": "cd .. && cd algo-visualizer && npm install --include=dev && npm run build",
    "install-backend": "npm install ",
    "dev": "npm run setup && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.0",
    "express": "^5.1.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
